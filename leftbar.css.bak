/* ==========================================================================
   CSS LEFTBAR - INDUSTRIAL STEALTH VERSION
   Note: Beberapa variabel diambil dari sistem :root Rightbar
   ========================================================================== 
*/

/* :root  di pusat style.css*/



/* --- CONTAINER UTAMA LEFTBAR --- */
.leftbar-container {
  width: var(--leftbar-width);
  height: 100vh;
  background: var(--bg-card);
  border-right: 1px solid var(--border-dim);
  display: flex;
  flex-direction: column;
  transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow-x: hidden;
  z-index: 1000;
  box-shadow: 5px 0 15px rgba(0, 0, 0, 0.3);
}

/* --- STATE: COLLAPSED (Mengecil) --- */
.leftbar-container.collapsed {
  width: var(--leftbar-collapsed-width);
}

/* --- TOMBOL TOGGLE (UI Atas) --- */
.btn-ui-toggle {
  background: transparent;
  border: none;
  color: var(--neon-blue);
  padding: 15px;
  cursor: pointer;
  font-family: 'Courier New', monospace;
  font-weight: bold;
  letter-spacing: 2px;
  text-align: right;
  transition: 0.3s;
  outline: none;
}

.btn-ui-toggle:hover {
  text-shadow: 0 0 8px var(--neon-blue);
  transform: translateX(3px);
}

/* --- HEADER & PROFIL --- */
.leftbar-header-wrapper {
  padding: 20px;
  text-align: center;
  border-bottom: 1px solid rgba(51, 65, 85, 0.5);
  margin-bottom: 10px;
}

.leftbar-header-title {
  color: var(--text-bright);
  font-size: 14px;
  letter-spacing: 2px;
  margin-bottom: 15px;
  line-height: 1.2;
}

.profile-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
}

.profile-img-container {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  border: 2px solid var(--neon-blue);
  object-fit: cover;
  box-shadow: 0 0 10px rgba(56, 189, 248, 0.2);
  transition: 0.3s;
}

/* Efek profil saat mengecil */
.leftbar-container.collapsed .profile-img-container {
  width: 35px;
  height: 35px;
}

/* --- MENU ITEMS --- */
.menu-group {
  padding: 10px 0;
}

.menu-item {
  display: flex;
  align-items: center;
  padding: 12px 20px;
  cursor: pointer;
  color: var(--text-dim);
  transition: all 0.2s ease;
  white-space: nowrap;
  gap: 15px;
}

.menu-item i {
  font-size: 18px;
  width: 25px;
  text-align: center;
  color: var(--neon-blue);
  transition: 0.3s;
}

.menu-item span {
  font-size: 14px;
  font-weight: 500;
}

.menu-item:hover {
  background: rgba(56, 189, 248, 0.1);
  color: var(--text-bright);
}

.menu-item:hover i {
  text-shadow: 0 0 10px var(--neon-blue);
}

/* --- BADGES (Admin & Sys) --- */
.badge-admin {
  background: var(--neon-gold);
  color: #000;
  font-size: 9px;
  padding: 1px 4px;
  border-radius: 3px;
  margin-left: 5px;
  text-transform: uppercase;
  font-weight: bold;
}

.badge-sys {
  background: var(--neon-purple);
  color: #fff;
  font-size: 9px;
  padding: 1px 4px;
  border-radius: 3px;
  margin-left: 5px;
  text-transform: uppercase;
}

/* --- LOGOUT BUTTON --- */
.bottom-nav {
  margin-top: auto;
  border-top: 1px solid var(--border-dim);
  padding-bottom: 20px;
}

.btn-logout {
  color: var(--neon-red) !important;
}

.btn-logout i {
  color: var(--neon-red) !important;
}

.btn-logout:hover {
  background: rgba(239, 68, 68, 0.1) !important;
}

/* --- HIDE TEXT WHEN COLLAPSED --- */
.leftbar-container.collapsed .leftbar-header-title,
.leftbar-container.collapsed span,
.leftbar-container.collapsed .badge-admin,
.leftbar-container.collapsed .badge-sys {
  opacity: 0;
  pointer-events: none;
  width: 0;
  display: none; /* Paksa hilang agar icon ke tengah */
}

.leftbar-container.collapsed .menu-item {
  justify-content: center;
  padding: 15px 0;
}

.leftbar-container.collapsed .menu-item i {
  margin: 0;
}

/* --- RESPONSIVE MOBILE OPTIMIZATION --- */
@media (max-width: 600px) {
    /* 1. Lebar Leftbar di HP lebih ramping */
    .leftbar-container {
        width: 180px; 
    }

    /* 2. Saat mengecil di HP, buat sangat minimalis (hanya ikon kecil) */
    .leftbar-container.collapsed {
        width: 60px;
    }

    .leftbar-container h3 {
        font-size: 1rem;
    }

    /* 3. Atur Padding di Rightbar (Main Content) agar tidak sempit */
    .inner-content {
        padding: 15px !important;
    }

    /* 4. Kecilkan ukuran Profile Image agar tidak makan tempat di HP */
    .profile-img-container {
        width: 45px;
        height: 45px;
    }
}